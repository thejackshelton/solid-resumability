// Entry point of the click page, resumable variant. Substituted for
// `click-classic.ts` by the plugin's HTML stage, which also inlines the
// emitted template into the page's `.mount`.
import { resumeAll } from "../click-page.ts";

void resumeAll();
