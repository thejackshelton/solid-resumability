// Entry point of the fixtures page, classic variant. `fixtures.html` names
// this file; the resumable build rewrites that one attribute to point at
// `fixtures-resumable.ts` instead. Everything else about the page is shared.
import { mountAll } from "../classic-page.ts";

mountAll();
