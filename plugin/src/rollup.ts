/** The Rollup entry. Analyze and emit run here unchanged; the page transforms are Vite-only until a slice gives them a trigger of their own. */
import unplugin from './index.ts';

export default unplugin.rollup;
